import { createContext } from "react";

const GlobalContext = createContext("Okayyy!"); // it stores this given value in object and return this object. Therefore GlobalContext is an object.

export default GlobalContext;
